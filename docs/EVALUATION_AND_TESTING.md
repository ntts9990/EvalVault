# Evaluation & Testing Guide

> **문서 버전**: 1.0.0
> **최종 업데이트**: 2025-12-30
> **Status**: RAG 최적화 측정 가이드 | 임베딩 벤치마크 | 테스트 커버리지
>
> *이 문서는 기존 RAG_OPTIMIZATION_GUIDE.md, EMBEDDING_BENCHMARK_PLAN.md, TEST_COVERAGE_PLAN.md를 통합한 문서입니다.*

---

## 개요

이 문서는 EvalVault의 평가/측정 기능과 내부 테스트 계획을 설명합니다.

> **EvalVault는 RAG 솔루션이 아닙니다.**
> 고객이 적용한 최적화 기법의 효과를 **객관적으로 측정**하고 **데이터 기반 의사결정**을 지원합니다.

```
┌─────────────────────────────────────────────────────────────┐
│                    EvalVault의 역할                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   고객의 RAG 최적화     EvalVault 측정      분석 보고서      │
│   (Reranker 적용)   →   (전후 비교)    →   (효과 검증)      │
│   (Chunking 변경)   →   (A/B 테스트)   →   (권장사항)       │
│   (Prompt 개선)     →   (메트릭 측정)  →   (인사이트)       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

# Part 1: RAG 최적화 효과 측정

## 1.1 측정 가능한 최적화 기법 (40개)

### 효과 측정 우선순위 평가 기준

| 기준 | 설명 |
|------|------|
| **Impact** | 성능 향상 폭 (측정 시 명확한 차이) |
| **Measurability** | EvalVault로 측정 가능한 정도 |
| **Actionability** | 측정 결과가 의사결정에 도움되는 정도 |
| **Evidence** | 연구에서 검증된 효과 |

## 1.2 Top 5 최적화 기법 측정 방법

### 1위: Hybrid Search (BM25 + Dense)

**측정 필요성**: 융합 검색이 실제로 효과가 있는지 검증

```bash
# 1. 기준선: BM25만 사용한 결과 평가
evalvault run bm25_results.json --metrics context_precision,context_recall

# 2. 비교군: Hybrid 적용 후 결과 평가
evalvault run hybrid_results.json --metrics context_precision,context_recall

# 3. 효과 비교
evalvault compare <bm25_run_id> <hybrid_run_id>
```

### 2위: Reranking (Cross-encoder)

**측정 필요성**: Reranker 추가 비용 대비 효과 검증

```bash
# A/B 테스트 설정
evalvault experiment-create "Reranker 효과 검증" \
    --hypothesis "Cross-encoder Reranker가 검색 정확도를 높인다"

evalvault experiment-add-group <exp_id> control --description "Reranker 없음"
evalvault experiment-add-group <exp_id> treatment --description "Reranker 적용"

# 통계적 비교
evalvault experiment-compare <exp_id> --test t-test
```

### 3위: Query Rewriting

**측정 필요성**: 쿼리 재작성이 실제 검색 품질을 개선하는지 검증

```bash
# 원본 쿼리 평가 결과
evalvault run original_query_results.json --metrics all

# 재작성된 쿼리 평가 결과
evalvault run rewritten_query_results.json --metrics all

# 상세 분석
evalvault analyze-compare <original_run_id> <rewritten_run_id>
```

### 4위: Chunking 전략

**측정 필요성**: 청킹 크기/방법에 따른 실제 성능 차이 검증

```bash
# 청킹 전략별 결과 평가 및 비교
evalvault run chunk_256_results.json --metrics all
evalvault run chunk_512_results.json --metrics all
evalvault run semantic_chunk_results.json --metrics all

# 종합 비교 분석
evalvault analyze <run_id> --report chunking_comparison.html
```

### 5위: Prompt Engineering

**측정 필요성**: 프롬프트 변경의 실제 효과 검증

```bash
# 프롬프트 A/B 테스트
evalvault experiment-create "프롬프트 CoT 효과"

# 기본 프롬프트 vs CoT 프롬프트 비교
evalvault experiment-compare <exp_id>
```

## 1.3 전체 최적화 기법 측정 가이드

| 순위 | 최적화 기법 | 측정 메트릭 | EvalVault 기능 |
|:---:|------------|-----------|---------------|
| 1 | Hybrid Search | Context Recall/Precision | compare |
| 2 | Reranking | Context Precision | experiment |
| 3 | Query Rewriting | Context Recall | analyze-compare |
| 4 | Chunking 전략 | All metrics | analyze |
| 5 | Prompt Engineering | Faithfulness | experiment |
| 6 | 임베딩 모델 | Recall/Precision | compare |
| 7 | HyDE | Context Recall | experiment |
| 8 | Multi-Query | Context Recall | analyze |
| 9 | Lost in the Middle | Faithfulness | experiment |
| 10 | Contextual Compression | All metrics | compare |

## 1.4 측정 워크플로우

```bash
# 1단계: 기준선 측정
evalvault run current_results.json --metrics all --output baseline

# 2단계: 최적화 적용 후 측정
evalvault run optimized_results.json --metrics all --output optimized

# 3단계: 효과 분석
evalvault compare <baseline_run_id> <optimized_run_id>

# 4단계: 통계적 검증
evalvault experiment-compare <exp_id> --test t-test

# 5단계: 보고서 생성
evalvault export <run_id> --format html --output optimization_report.html
```

## 1.5 측정 결과 해석 가이드

### 통계적 유의성 판단

| p-value | 해석 | 권장 액션 |
|---------|------|----------|
| < 0.01 | 매우 유의미 | 적용 권장 |
| 0.01 ~ 0.05 | 유의미 | 추가 테스트 후 결정 |
| 0.05 ~ 0.10 | 경계선 | 더 많은 샘플로 재테스트 |
| > 0.10 | 유의미하지 않음 | 다른 최적화 검토 |

### 효과 크기 판단

| 변화율 | 해석 | 비용 대비 판단 |
|--------|------|--------------|
| > 20% | 큰 효과 | 추가 비용 감수 가능 |
| 10~20% | 중간 효과 | ROI 분석 필요 |
| 5~10% | 작은 효과 | 저비용일 경우만 적용 |
| < 5% | 미미한 효과 | 적용 불필요 |

---

# Part 2: 임베딩 모델 벤치마크 프레임워크

## 2.1 개요

> **EvalVault는 임베딩 모델을 제공하지 않습니다.**
> 고객이 사용 중인 임베딩 모델의 성능을 **객관적으로 측정**하고 **최적 모델 선택을 지원**합니다.

### 측정 대상

| 모델 유형 | 예시 | 측정 가능 |
|----------|------|:--------:|
| **OpenAI 계열** | text-embedding-3-small/large, ada-002 | ✅ |
| **한국어 특화** | BGE-m3-ko, KoSimCSE, ko-sroberta | ✅ |
| **다국어** | multilingual-e5, BGE-m3 | ✅ |
| **로컬/폐쇄망** | Qwen3-Embedding (Ollama) | ✅ |
| **커스텀** | Fine-tuned 모델 | ✅ |

### 핵심 질문에 답변

| 질문 | EvalVault가 제공하는 답 |
|------|----------------------|
| "어떤 임베딩 모델이 우리 도메인에 적합한가?" | 도메인 데이터 기반 성능 비교 |
| "차원을 줄이면 성능이 얼마나 떨어지나?" | Matryoshka 차원별 성능 분석 |
| "비용 대비 성능이 좋은 모델은?" | ROI 분석 보고서 |

## 2.2 벤치마크 항목

### 표준 벤치마크

| 벤치마크 | 측정 항목 | 활용 |
|---------|---------|------|
| **AutoRAG** | Top-k 1, NDCG@10 | 한국어 검색 품질 표준 |
| **MIRACL** | NDCG@10 | 국제 표준 대비 성능 |

### 도메인 특화 벤치마크

| 항목 | 설명 | 중요도 |
|------|------|:------:|
| **검색 Recall@5** | 상위 5개 결과 중 정답 포함률 | ★★★★★ |
| **검색 Precision@5** | 상위 5개 결과의 정확도 | ★★★★☆ |
| **의미 유사도** | 질문-답변 쌍의 의미 유사도 | ★★★☆☆ |
| **처리 속도** | 문서당 임베딩 생성 시간 | ★★★☆☆ |

## 2.3 Matryoshka 차원별 성능 분석

차원 축소가 성능에 미치는 영향을 분석합니다.

| 목적 | 설명 |
|------|------|
| **비용 최적화** | 저차원으로 저장 공간/비용 절감 가능성 분석 |
| **성능 트레이드오프** | 차원 축소 시 성능 저하 정도 파악 |
| **최적 차원 결정** | 성능/비용 균형점 도출 |

### 차원별 분석 예시

```
┌─────────┬──────────┬───────────┬───────────┐
│ 차원    │ Recall@5 │ 저장공간  │ 성능유지율│
├─────────┼──────────┼───────────┼───────────┤
│ 4096    │ 0.82     │ 16KB/doc  │ 100%      │
│ 1024    │ 0.80     │ 4KB/doc   │ 97.6%     │
│ 512     │ 0.78     │ 2KB/doc   │ 95.1%     │
│ 256     │ 0.74     │ 1KB/doc   │ 90.2%     │
│ 128     │ 0.68     │ 0.5KB/doc │ 82.9%     │
└─────────┴──────────┴───────────┴───────────┘

💡 권장사항:
프로덕션: 1024차원 (97.6% 성능, 75% 저장공간 절감)
비용 민감: 512차원 (95% 성능, 87.5% 저장공간 절감)
개발/테스트: 256차원 (90% 성능)
```

## 2.4 벤치마크 사용 방법

```bash
# 고객의 각 임베딩 모델로 검색 결과 생성 후 평가

# 모델 A 결과 평가
evalvault run model_a_results.json \
    --metrics context_recall,context_precision \
    --output model_a_benchmark

# 모델 B 결과 평가
evalvault run model_b_results.json \
    --metrics context_recall,context_precision \
    --output model_b_benchmark

# 모델 비교
evalvault compare <model_a_run_id> <model_b_run_id>

# A/B 테스트 (통계적 유의성 검증)
evalvault experiment-create "임베딩 모델 비교: BGE vs Qwen"
evalvault experiment-compare <exp_id> --test mann-whitney
```

## 2.5 환경별 권장 모델

| 환경 | 권장 모델 | 이유 |
|------|---------|------|
| 인터넷 + GPU | BGE-m3-ko | 최고 성능 |
| 폐쇄망 + GPU | Qwen3-Embedding-8B | BGE 대비 97.6% |
| 폐쇄망 + 저사양 | Qwen3-Embedding-0.6B | 개발/테스트용 |
| API 선호 | OpenAI text-embedding-3-small | 간편 |

---

# Part 3: 테스트 커버리지 계획 (완료)

> **Status (2025-12-28)**: 이 계획은 성공적으로 완료되었습니다.
> - 전체 커버리지: 53% → **89%** 달성
> - CLI 커버리지: 46% → **89%** (목표 80% 초과 달성)
> - 총 테스트 수: **457개** (Unit 431 + Integration 26)

## 3.1 커버리지 현황 (계획 시점)

- **CLI**: Typer 커맨드 다수 0%
- **Dataset Loaders**: 0~82% 산재
- **Knowledge Graph**: 55-95% 편차
- **Langfuse/Storage/LLM 어댑터**: 다수 0%

## 3.2 목표

| Sprint | 범위 | 목표 |
|--------|------|------|
| 1주차 | CLI + Dataset Loaders | 80%+ |
| 2주차 | KG Generator + Experiment Manager | 85%+ |
| 3주차 | Langfuse/Storage/LLM + Config | 75%+ |

## 3.3 실행 계획

### CLI 커맨드 (Typer)

- **전략**: `typer.testing.CliRunner` 사용
- **대상**: `run`, `kg_stats`, `compare`, `experiment_*`, `generate`
- **완료 조건**: CLI 파일 전체 커버리지 80% 이상

### Dataset Loaders

- **전략**: 개별 서브루틴 검증
- **시나리오**: 정상 로딩, 존재하지 않는 경로, 잘못된 인코딩
- **완료 조건**: 각 loader 85% 이상

### Knowledge Graph & Generator

- **전략**: 그래프 빌드/경로 탐색/질문 생성 테스트
- **완료 조건**: `kg_generator.py` 주요 메서드 90% 이상

### Langfuse / Storage / LLM Adapters

- **전략**: MagicMock으로 외부 의존성 모킹
- **완료 조건**: 각 어댑터 모듈 70% 이상

## 3.4 결과

```
최종 테스트 커버리지 (2025-12-28)
═══════════════════════════════════════════════════════════════

┌────────────────────────┬─────────┬─────────┬─────────┐
│ 영역                   │ 계획 전 │ 계획 후 │ 목표    │
├────────────────────────┼─────────┼─────────┼─────────┤
│ 전체 커버리지          │ 53%     │ 89%     │ 75%+ ✅ │
│ CLI                    │ 46%     │ 89%     │ 80%+ ✅ │
│ Dataset Loaders        │ 60%     │ 88%     │ 85%+ ✅ │
│ KG Generator           │ 75%     │ 92%     │ 90%+ ✅ │
│ Adapters               │ 40%     │ 85%     │ 70%+ ✅ │
└────────────────────────┴─────────┴─────────┴─────────┘

총 테스트 수: 457개 (Unit 431 + Integration 26)
```

---

# 보고서 예시

## RAG 최적화 효과 종합 보고서

```
═══════════════════════════════════════════════════════════════
              RAG 최적화 효과 종합 보고서
═══════════════════════════════════════════════════════════════

📊 테스트 개요
   - 테스트 데이터셋: insurance_qa_korean.json (200 QA pairs)
   - 평가 메트릭: Faithfulness, Context Recall, Context Precision

═══════════════════════════════════════════════════════════════

📈 최적화 효과 요약

┌────────────────────┬──────────┬──────────┬─────────┬──────────┐
│ 최적화 기법        │ 기준선   │ 적용 후  │ 변화    │ 유의성   │
├────────────────────┼──────────┼──────────┼─────────┼──────────┤
│ Hybrid Search      │ 0.68     │ 0.82     │ +20.6%  │ ✅ p<0.01│
│ Reranker           │ 0.72     │ 0.86     │ +19.4%  │ ✅ p<0.01│
│ CoT Prompt         │ 0.72     │ 0.78     │ +8.3%   │ ✅ p<0.05│
│ Query Rewriting    │ 0.62     │ 0.75     │ +21.0%  │ ✅ p<0.01│
└────────────────────┴──────────┴──────────┴─────────┴──────────┘

═══════════════════════════════════════════════════════════════

💡 권장사항

적용 권장:
✅ Hybrid Search - 검색 품질 20% 향상
✅ Reranker - 정확도 19% 향상

추가 테스트 필요:
🔬 CoT Prompt - 응답 시간 증가 대비 효과 재검토

═══════════════════════════════════════════════════════════════

Generated by EvalVault
```

---

# 참고 문헌

1. Gao, L. et al. (2024). "Retrieval-Augmented Generation for Large Language Models: A Survey"
2. Ma, X. et al. (2023). "Query Rewriting for Retrieval-Augmented Large Language Models"
3. Liu, N. et al. (2023). "Lost in the Middle: How Language Models Use Long Contexts"

---

# 참고 문서

- [FEATURE_GUIDE.md](./FEATURE_GUIDE.md) - EvalVault 기능 가이드
- [USER_GUIDE.md](./USER_GUIDE.md) - 사용자 가이드
- [KOREAN_RAG_OPTIMIZATION.md](./KOREAN_RAG_OPTIMIZATION.md) - 한국어 RAG 최적화 가이드
