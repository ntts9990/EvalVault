# Future Tests

- Manual: run `uv run evalvault serve-api --reload` plus `cd frontend && npm run dev` and verify Dashboard filters (date range + project), trend charts, and deltas.
- Manual: start a new evaluation with a project label and confirm it appears in Dashboard filters and run cards.
- Manual: open Customer Report (`/reports`), apply filters, and verify charts + summary table.
